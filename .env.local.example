# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (REQUIRED for account deletion feature)
# ⚠️ WARNING: This key bypasses Row Level Security. Keep it SECRET and server-side only!
#
# Required for:
# - Account deletion (deleteAccount server action)
# - Admin operations that need elevated permissions
#
# How to obtain:
# 1. Go to your Supabase project dashboard
# 2. Navigate to: Project Settings > API
# 3. Copy the "service_role" key (NOT the "anon" key)
# 4. Paste it below
#
# IMPORTANT: Never commit this key to version control or expose it to the client!
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# OpenAI Configuration
OPENAI_API_KEY=your-openai-api-key-here

# OpenAI Model Configuration
# Options: gpt-5-mini (default, balanced cost/performance), gpt-5.1 (latest, recommended for production)
# The model can be configured globally via this env variable
# Individual agents may override with specific reasoning/verbosity settings
OPENAI_MODEL=gpt-5.1

# ExerciseDB API Configuration (for exercise animations)
# Option 1: Self-hosted (RECOMMENDED - free, unlimited, no API key needed)
# Deploy from: https://github.com/ExerciseDB/exercisedb-api
# Then set your Vercel URL here:
NEXT_PUBLIC_EXERCISEDB_API_URL=https://your-exercisedb.vercel.app

# Option 2: RapidAPI (if using paid tier)
# NEXT_PUBLIC_EXERCISEDB_API_URL=https://exercisedb.p.rapidapi.com
# NEXT_PUBLIC_EXERCISEDB_API_KEY=your-rapidapi-key-here
